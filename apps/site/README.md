# apps/site

This folder is the target home for the zacksimon.dev frontend app in the monorepo.

Current production site files still live at the repository root. Migrate root static assets into this folder during the next migration phase once deploy pipelines are updated to publish from `apps/site`.
